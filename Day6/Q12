#include <stdio.h>
int main()
{
    int num;
    printf("Enter number : ");
    scanf("%d", &num);

    if (num == 0)
    {
        printf("The number is ZERO ");
    }
    else if (num % 2 == 0)
    {
        printf("The number is EVEN");
    }
    else
        printf("The number is odd");
}

#include <stdio.h>
int main ()
{
  int a,b;
  printf("Enter number : ");
  scanf("%d",&a);
  printf("Enter number : ");
  scanf("%d",&b);
  a+=b;
  b+=a;
  a=b-a;
  b= b -a -a;
  printf("%d %d ", a, b);
  return 0;
}

#include <stdio.h>
int main()
{
    int n;
    float product = 1;
    printf("Enter number ");
    scanf("%d", &n);

    for (int i = 2; i <= n; i+=2)
    {
        product *= i;
    }

    printf("Product of even number till %d is %.2f", n, product);
}
